{
  "name": "ccccg",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "build:manifest": "node tools/generate-asset-manifest.mjs",
    "build:client": "esbuild src/client-entry.js --bundle --format=esm --minify --outfile=scripts/client-bundle.js",
    "build:catalog": "node tools/build-gear-catalog.mjs",
    "build": "npm run build:client && npm run build:catalog && npm run build:manifest"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.5",
    "node-fetch": "^3.3.2"
  },
  "jest": {
    "testEnvironment": "jsdom"
  },
  "dependencies": {
    "motion": "^12.23.18",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
