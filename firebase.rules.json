{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.isDM === true)",
        ".write": "auth != null && (auth.uid === $uid || auth.token.isDM === true)"
      }
    }
  }
}
